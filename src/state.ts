import {initialLayout,validLayout,BUDGET,changeCost,END,type Layout} from './simulation';
export type Save={version:1;base:Layout;draft:Layout;remaining:number;time:number;selected:number;count:number;history:Layout[]};
export const fresh=():Save=>({version:1,base:initialLayout(),draft:initialLayout(),remaining:BUDGET,time:0,selected:0,count:500,history:[]});
export function parseSave(raw:string):Save{const s=JSON.parse(raw);if(s.version!==1||!validLayout(s.base)||!validLayout(s.draft)||!Number.isFinite(s.remaining)||s.remaining<0||s.remaining>BUDGET||!Number.isFinite(s.time)||s.time<0||s.time>END||!Number.isInteger(s.selected)||s.selected<0||s.selected>2||![500,2000,5000].includes(s.count)||!Array.isArray(s.history)||s.history.length>100||!s.history.every(validLayout)||(!s.draft.unlimited&&changeCost(s.base,s.draft)>s.remaining))throw Error('Dieser Spielstand ist ungültig oder stammt aus einer anderen Version.');return s;}
export function edit(s:Save,layout:Layout):Save{if(!validLayout(layout))throw Error('Ungültiger Entwurf.');if(!layout.unlimited&&changeCost(s.base,layout)>s.remaining)throw Error('Das Baubudget reicht für diesen Entwurf nicht.');return {...s,draft:structuredClone(layout),history:[...s.history.slice(-99),structuredClone(s.draft)],time:0};}
export function commit(s:Save):Save{const cost=s.draft.unlimited?0:changeCost(s.base,s.draft);return {...s,base:structuredClone(s.draft),remaining:s.remaining-cost,history:[],time:0};}
