import {simulate,initialLayout} from '../src/simulation';import os from 'node:os';
const rows=[];for(const count of [500,2000,5000]){const start=performance.now();const r=simulate({...initialLayout(),street:'bike',links:['park-east'],park:'garden',entrance:'east'},count);rows.push({count,ms:+(performance.now()-start).toFixed(2),onTime:r.onTime,visits:r.visits,unreachable:r.unreachable,serializedMiB:+(JSON.stringify(r).length/1048576).toFixed(2),heapMiB:+(process.memoryUsage().heapUsed/1048576).toFixed(1)});}console.log(JSON.stringify({cpu:os.cpus()[0].model,platform:os.platform(),node:process.version,scope:'Simulation only; whole 90-minute morning, no rendering',rows},null,2));
